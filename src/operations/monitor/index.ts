/**
 * Session Monitor module
 *
 * Provides SessionMonitor for periodic idle session cleanup
 * and sticky message refresh.
 */

export { SessionMonitor } from './handler.js';
export type { SessionMonitorOptions } from './handler.js';
